---
title: Hugo directory based archetype
---

I added a [directory based archetype](https://gohugo.io/content-management/archetypes/) for creating new notebook pages. Using `.Name` in the archetype gives the name of the new object so I could make the directory and the `title` and `date` in frontmatter all the same.