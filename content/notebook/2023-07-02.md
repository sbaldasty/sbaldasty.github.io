## Personal website

Using the `.Pages` variable in the context of a [hugo page](https://gohugo.io/variables/page/) gives back all the child pages, versus going through `.Paginator` whose pages are limited.

## Coding session

Mpyc does not support `//` for integer division, have to use `mpc.div` instead.

Mpyc does not limit comparison output values to {-1, 0, 1}, instead it appears to rely on the sign.

## Proof assistants

